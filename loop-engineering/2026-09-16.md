# 2026-09-16

- **[実践][停止条件] Workflowがsession-limitで4体を同時killし、24体完了済みの成果(合成前の分析約380KB)も4日間ロストしたまま——モデルに残り予算が見えず大きく計画してから死ぬ** — https://github.com/anthropics/claude-code/issues/94577
  - **何が起きたか**: 4体のマルチエージェントWorkflow run(268ターン・3.3MB)がsession-limitで同時killされ、`journal.jsonl`に結果エントリが0件のまま消滅。別の24体runは全エージェント完了(24/24)したのに合成(synthesis)直前でクランプされ、約380KB分の完成済み分析が4日間ロストした。(2026-09-15報告、Claude Code Desktop・Windows・Maxプラン、Issue open)
  - **なぜ注目されているか**: 09-02〜09-15付ダイジェストが扱ってきた「上限到達後も止まらない」系統の裏にある根本原因を指摘——モデルは自分のセッション/週次の残り予算を一切見えないまま計画するため、180kトークンしか残っていなくても24体規模のfan-outを平然と組んでしまう。対症療法ではなく可視性そのものの欠落を具体的な数字で示した。
  - **自分の設計への示唆**: 出題生成・検証のWorkflowをrecursive(生成→検証→修正の反復)で組む設計は、rate limitが「最後のラウンドの真っ最中」に落ちやすく、収束する直前の回だけ毎回失われうると知っておく。恒久対策が無い前提で、各フェーズ開始前にトークン使用量を自前でログし、残り予算の目安(例:セッション上限の7割消費で新規fan-outを止める)をハードコードしたガードとして入れる。

- **[実践][実装] Workflowのbare `agent()`は公式ドキュメントの推奨通り`model`省略で1日41回spawnすると$643.72(全サブエージェント支出の54%)——同じトークン量でもSonnetなら$170.76、Haikuなら$85.38で済んだ** — https://github.com/anthropics/claude-code/issues/94534
  - **何が起きたか**: Workflowスクリプトのドキュメントは`opts.model`を省略しセッションモデルへ継承させることを推奨しているが、各agent spawnにはキャッシュ読み込み5万〜18万+キャッシュ書き込み4万〜14万トークンのpreambleが乗り、最上位モデルでの実行はHaikuの約9倍のコストになる。実測では1日で41回のbare `agent()`呼び出しが$643.72(全サブエージェント支出の54%)を消費、同じトークン量でもSonnetなら$170.76、Haikuなら$85.38で済んでいた。(2026-09-15報告、v2.1.271、Issue open)
  - **なぜ注目されているか**: 09-08〜09-11付ダイジェストで扱ってきた「effort/modelが親から意図せず継承される」系統は主にバグとして報告されてきたが、今回は公式ドキュメントの推奨文言自体が「省略すればほぼ常に正しい」と書きながら実際には最も高価な選択になるという、ガイダンスとコストの実測が食い違っている点を指摘した。
  - **自分の設計への示唆**: 出題生成・検証をWorkflowの`agent()`で組む設計は、ドキュメントの「modelを省略してよい」を鵜呑みにせず、各フェーズで実際に必要なモデル階層(出題生成はSonnet級、簡易フォーマットチェックはHaiku級など)を毎回明示指定する。指定を怠ると気づかぬまま最上位モデルのpreambleコストを毎回払い続けることになる。

- **[実装] バックグラウンドdispatchしたAgentが親セッションのcontext compactionでセッションIDが変わると、ディスク上にtranscriptが完全に残っていても`SendMessage`で二度と再開できなくなる** — https://github.com/anthropics/claude-code/issues/94267
  - **何が起きたか**: `run_in_background: true`で起動したAgentが完了しても、起動元セッションがcontext compactionで新しいセッションIDにrolloverすると`<task-notification>`が届かず、`SendMessage({to: agentId})`は「transcriptが見つからない」と失敗する。実際には旧セッションID配下に218KBの完全なtranscriptが残っており、検索範囲が現在のセッションディレクトリに限定されているのが原因と特定された。(2026-09-14報告、v2.1.270・Linux・sdk-ts、報告者の利用で5回以上再発、Issue open、再現手順あり)
  - **なぜ注目されているか**: 09-13付ダイジェストで扱った「サブエージェント終了後もbackground Bashが親を失ったまま残り続ける」#93889とは逆方向で、今回はプロセスもtranscriptも正常に残っているのに、親側の検索ロジックがセッション単位に限定されているせいで結果を回収する手段が無くなるという、より基本的な設計の穴。
  - **自分の設計への示唆**: 出題生成・検証を長時間セッションでバックグラウンドAgentに任せる設計は、途中でcontext compactionが起きると起動済みのAgentの結果を`SendMessage`経由では二度と回収できなくなりうると知っておく。回避策は、Agentの結果をtranscript任せにせず、完了時に必ず既知のファイルパス(例:`results/<agentId>.json`)へ書き出させる設計にし、`SendMessage`が失敗した場合はそのファイルを直接読みに行くフォールバックを用意する。

- **[実践][停止条件] Workflowがsession-limit到達後も334体を空振り追加dispatchし、実際には一度も検証していない111件の指摘を全て「反証済み」と記録——判定不能と誤判定の区別が無いまま確信度の高い誤った結果になる** — https://github.com/anthropics/claude-code/issues/94530
  - **何が起きたか**: 9体のFind役が発見した111件の指摘に対し、Verify役333体をsession-limit到達後もdispatchし全滅(1件も実行されず)させた結果、`confirmed: 0, refuted: 111`と記録され、177件の指摘が「反証されて消えた」ことにされた。実際は反証が一度も行われておらず、runは`status: "completed"`と報告された。(2026-09-14実行・09-15報告、Max 20xプラン・claude-fable-5-1、Issue closed)
  - **なぜ注目されているか**: 09-13付#93874(189体空振り)と同一の根本原因だが、今回は「quota到達で失敗したエージェントの結果」と「実際に判定して否定した結果」をスクリプト側が区別できず、177件の正当な指摘が静かに"反証済み"として握りつぶされるという、上限到達の被害が単なるトークン浪費ではなく結果の信頼性そのものを壊す形で顕在化した点が新しい。
  - **自分の設計への示唆**: 出題の生成をAIに検証させる設計(まさにこのFind→Verifyと同型)は、検証役がquotaや上限で実行されなかった場合に「不正解」「検証NG」と誤って記録しないよう、`agent()`の戻り値がquota失敗によるものか実際の判定結果かを明示的に区別してから合否を確定させる。判定不能なものは「不正解」に倒さず「要再検証」の第三状態として扱い、静かに握りつぶさない設計にする。
